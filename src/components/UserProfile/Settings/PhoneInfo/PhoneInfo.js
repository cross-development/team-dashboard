//Core
import React from 'react';
import PropTypes from 'prop-types';
//Styles
import { StyledPhoneInfo, StyledLabel } from './PhoneInfo.styles';

const PhoneInfo = () => {
	return (
		<StyledPhoneInfo>
			<StyledLabel></StyledLabel>
		</StyledPhoneInfo>
	);
};

PhoneInfo.propTypes = {};

export default PhoneInfo;
