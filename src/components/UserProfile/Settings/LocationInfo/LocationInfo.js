//Core
import React from 'react';
import PropTypes from 'prop-types';
//Styles
import { StyledLocationInfo, StyledLabel } from './LocationInfo.styles';

const LocationInfo = () => {
	return (
		<StyledLocationInfo>
			<StyledLabel></StyledLabel>
		</StyledLocationInfo>
	);
};

LocationInfo.propTypes = {};

export default LocationInfo;
